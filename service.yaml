apiVersion: v1
kind: Service
metadata:
  name: calculator-service
spec:
  type: NodePort
  selector:
    app: simple-interest-calculator
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
